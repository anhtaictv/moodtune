# Secrets - do not commit
backend/spotify_tokens.json

# Baseline giả lập bộ đếm online/listening — chỉ chạy local/server, không push
backend/fake_presence.py

# Python
__pycache__/
*.pyc
.pytest_cache/

# Logs
*.log

# Snapshot trước mỗi lần dạy AI hàng loạt (xem teacher_common.backup_model_state)
backend/backups/

# Cache đặc trưng audio (BPM/Spectral/MFCC) — tự sinh lại được, không cần track
backend/audio_cache.json

# Claude Code local settings
.claude/
.agents/
.impeccable/
skills-lock.json

# Temp tooling used to set up this repo
.tmp_gh/

# Runtime state (auto-generated, không cần track)
backend/feedback_log.jsonl
backend/visit_stats.json
backend/bandit_state.json

# Weight backups (dùng backend/backups/ thay thế)
backend/weights_prod_backup_*.npz
backend/weights_prod_backup_*.json
backend/weights_feedback_test*

# Temp/check files
check*.txt
dist_check.txt
retrain_*.txt
debug_*.js
teach.html
